# Decisions Log

| # | Decision | Rationale | Date |
|---|----------|-----------|------|
| 1 | Using uv for environment management | Available on system, fast, handles Python version management | Build start |
| 2 | YOLOv8n pretrained (COCO) for vehicle detection | COCO already contains car/bus/truck/motorcycle classes; plate detection via dedicated model or vehicle crop fallback | Build start |
| 3 | SQLite for database | Simple, no server needed, perfect for demo | Build start |
| 4 | **EasyOCR** instead of PaddleOCR | PaddleOCR 2.10 has torch DLL conflicts on Windows (`shm.dll` WinError 127). EasyOCR installed cleanly and works with torch 2.5.1+cpu. Trade-off: slightly lower OCR accuracy on synthetic plates, but functional end-to-end. | Build - PaddleOCR failed |
| 5 | DeepSORT via deep-sort-realtime for per-camera tracking | Lightweight, well-tested, works in real-time. Note: the tracker is DeepSORT (with a MobileNetV2 embedder); it is **not** ByteTrack. | Build start |
| 6 | RapidFuzz for fuzzy plate matching | Fast C-based Levenshtein, better than python-Levenshtein for our needs | Build start |
| 7 | Pretrained yolov8n.pt for vehicle detection only | No fine-tuned plate model available yet; plate detection disabled at detection stage, downstream OCR attempts on vehicle crops instead | Build start |
| 8 | Synthetic test videos | No real traffic footage available; created programmatically generated videos with labeled plates for end-to-end pipeline testing. Real footage needed for demo. | Build start |
| 9 | torch 2.5.1+cpu pinned | torch 2.14.0 caused WinError 127 on shm.dll. Downgrading to 2.5.1+cpu resolved the issue. | Build - torch fix |
| 10 | start_demo.py (Python) as primary launcher | Cross-platform; start_demo.sh also provided for Linux/macOS/WSL | Build start |
| 11 | Single shared fusion service via SQLite | Camera workers only persist sightings (with `track_id`); one fusion engine (`src.fusion.engine`, run as `python -m src.fusion.worker`, at end-of-run, or via `POST /fusion/run`) consumes them and rebuilds `trajectories` + `trajectory_sightings` inside a `BEGIN IMMEDIATE` transaction. No per-worker fusion state. | Multi-process audit |
| 12 | Chronological ordering enforced in fusion | A transition A→B requires `timestamp_B > timestamp_A`. Zero elapsed time is rejected (removes the unsafe "zero gap is always valid" case). Fusion always traverses sightings chronologically. | 2nd audit |
| 13 | Multi-signal association scoring | `score = 0.40·plate_similarity + 0.20·ocr_confidence + 0.30·spatiotemporal + 0.10·vehicle_attribute`. Hard rejects: edit distance > 2, non-positive elapsed time, implied speed > 150 km/h, same-camera different-track, vehicle-class conflict. | 2nd audit |
| 14 | Canonical-plate coherence in trajectory building | A candidate may join a trajectory only if its plate is within edit distance 2 of the trajectory's first plate. Prevents one weak intermediate reading from chaining two unrelated vehicles together (union-find transpartment/transitive-merge hazard). | 2nd audit |
| 15 | Track-aware OCR voting | Voting history is keyed by logical vehicle identity `(camera_id, track_id)` so different vehicles never share a vote; TTL 30s, bounded 256 tracks. Returned `confidence` always matches the returned (voted) text. | 2nd audit |
| 16 | Speed/density measured honestly | `compute_speed` returns `None` (not a fabricated `0 km/h`) when calibration or positive elapsed time is missing. Density/congestion use unique active track IDs where available, with documented proxy fallbacks (<=2 low, <=6 medium, else high). | 2nd audit |
| 17 | OCR confidence gate 0.50 | `min_ocr_confidence` is a single consistent constant (0.50) across pipeline, tests and docs. No real-footage plate reads passed this gate; that is documented honestly in VERIFICATION.md, not faked. | 2nd audit |
| 18 | Stable `.pt` resolution for HF plate weights | `hf_hub_download` returns a `blobs/<hash>` path Ultralytics rejects; `resolve_plate_model_from_hub` now resolves via sibling `snapshots/*/<file>` or a deterministic copy to `models/detection/<file>`, printing path/suffix/size/sha256 and hard-failing unless suffix is `.pt`. | Colab live fix |
| 19 | Full-frame plate fallback (cached per frame) | When in-crop localization finds nothing, one full-frame plate pass per frame is associated back to vehicles via IoU; hits counted separately (`plate_fullframe_fallback_used`) so the diagnostic shows which path produced each box. | Colab live fix |
| 20 | Position-aware confusion correction + per-position fusion | `position_correct` fixes only class-violating characters per Indian-plate segment (fewest-changes valid split wins); `fuse_track_observations` votes per position with confidence weights over same-length compatible strings, else whole-string fallback. Covered by `tests/test_per_position_voting.py`. | Colab live fix |
| 21 | Debug-crop saving (bounded) | `PipelineRunner(debug_crops_dir=...)` saves ≤5 crops/category (`crop`, `tiny_*`) plus annotated frames to Drive for visual failure attribution. | Colab live fix |
| 22 | Constructor-level `.pt` enforcement (blob-path regression) | `PlateDetector.__init__` routes every plate path through `_require_pt_weights` (blob → snapshot/copy or loud `RuntimeError`); `_load_yolo` refuses non-`.pt` paths before Ultralytics is touched. Regression covered by `tests/test_plate_model_path.py` with a fake hub layout. No filter logic changed. | Colab live fix |
| 23 | Candidate ledger (diagnostic-only, thresholds frozen) | `PipelineRunner` records one ledger row per evaluated plate box (frame, track, bbox, det conf, crop size, aspect, bumper verdict, raw/pre/accept OCR outputs, correction, regex, final reason) plus CSV + run-end det/OCR distributions. Explicit counters `candidate_boxes/aspect_rejected/position_rejected/tiny_rejected/detector_conf_rejected/ocr_attempted/ocr_empty/ocr_low_conf/regex_rejected/accepted`; variant crops saved ≤5/category. Covered by `tests/test_candidate_ledger.py`. No gate changed. | Colab live diag |
